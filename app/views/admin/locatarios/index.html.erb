<%# app/views/admin/locatarios/index.html.erb %>

<p style="color: green"><%= notice %></p>

<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="fw-bold text-primary">Locatários</h1>
</div>

<div class="row row-cols-1 row-cols-md-3 g-4" id="locatarios">
  <% if @locatarios.any? %>
    <% @locatarios.each do |locatario| %>
      <%= render "locatario", locatario: locatario %>
    <% end %>
  <% else %>
    <div class="text-center text-muted mt-5">
      <h5>Nenhum locatário encontrado 😕</h5>
      <p>Adicione um novo locatário para começar.</p>
    </div>
  <% end %>
</div>

<!DOCTYPE html>
<html>
  <head>
    <title>ADMIN - Locadora</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_importmap_tags %>
  </head>
  <body>

    <%# --- LAYOUT DE ADMIN (SOMENTE LINKS DE ADMIN) --- %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "ADMIN", admin_carros_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <%# Links de gerenciamento %>
            <li><%= link_to "Gerenciar Carros", admin_carros_path, class: "nav-link" %></li>
            <li><%= link_to "Gerenciar Locatários", admin_locatarios_path, class: "nav-link" %></li>
            <li><%= link_to "Gerenciar Empréstimos", admin_emprestimos_path, class: "nav-link" %></li>
          </ul>

          <%# Links de Admin na direita %>
          <ul class="navbar-nav">
            <li class="nav-item">
              <span class="navbar-text text-white me-2">
                Admin: <%= current_locatario.nome %>
              </span>
            </li>
            <li class="nav-item">
              <%# Link para voltar ao site público %>
              <%= link_to "Ver Site", root_path, class: "nav-link", target: "_blank" %>
            </li>
            <li class="nav-item">
              <%# --- ESTA É A CORREÇÃO --- %>
              <%# Removemos o 'data: { turbo_method: :delete }' %>
              <%= link_to "Sair", destroy_locatario_session_path, class: "nav-link" %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <%# --- FIM DA BARRA DE MENU ADMIN --- %>

    <div class="container mt-3">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </div>

    <main class="container">
      <%= yield %>
    </main>
    
  </body>
</html>
<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Locadora de Carros" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Importando Bootstrap 5 -->
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js", crossorigin: "anonymous" %>

    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light" style="padding-top: 70px;"> 
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
      <div class="container">
        <%= link_to "Locadora Rails", root_path, class: "navbar-brand fw-bold" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Ver Carros", root_path, class: "nav-link" %>
            </li>
          </ul>

          <ul class="navbar-nav">
            <% if locatario_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text text-white me-3">
                  👋 Olá, <%= current_locatario.nome %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to "Minha Conta", edit_locatario_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Sair", destroy_locatario_session_path, class: "nav-link" %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Login", new_locatario_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Cadastre-se", new_locatario_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Flash messages -->
    <div class="container mt-3">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
    </div>

    <!-- Conteúdo principal -->
    <main class="container py-4">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-dark text-white-50 text-center py-3 mt-auto">
      <div class="container">
        <small>© <%= Time.current.year %> Locadora Rails — Todos os direitos reservados</small>
      </div>
    </footer>
  </body>
</html>

<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Site Locadora Carros" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= javascript_importmap_tags %>
  </head>
  <body>

    <%# --- LAYOUT PÚBLICO (SEM LINKS DE ADMIN) --- %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "Locadora Rails", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <%# O único link de navegação é o público %>
            <%= link_to "Ver Carros", root_path, class: "nav-link" %>
          </ul>

          <%# Links de Login/Logout na direita %>
          <ul class="navbar-nav">
            <% if locatario_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text text-white me-2">
                  Olá, <%= current_locatario.nome %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to "Minha Conta", edit_locatario_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%# Esta é a correção do Logout (sem o 'data: ...') %>
                <%= link_to "Sair", destroy_locatario_session_path, class: "nav-link" %>
              </li>
            <% else %>
              <%# Links para quem NÃO ESTÁ LOGADO %>
              <li class="nav-item">
                <%= link_to "Login", new_locatario_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Cadastre-se", new_locatario_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <%# --- FIM DA BARRA DE MENU PÚBLICA --- %>

    <div class="container mt-3">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </div>

    <main class="container">
      <%= yield %>
    </main>
    
  </body>
</html>
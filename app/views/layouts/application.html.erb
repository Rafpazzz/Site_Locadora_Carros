<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Site Locadora Carros" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# 
      ESTA É A CORREÇÃO:
      Trocamos :app (um símbolo) por "application" (uma string), 
      que é o nome real do seu arquivo de CSS (application.scss).
    %>
    
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <%# --- 1. BARRA DE NAVEGAÇÃO COMPLETA (BOOTSTRAP) --- %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "Locadora Rails", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <%# Links para todos %>
            <%= link_to "Ver Carros", root_path, class: "nav-link" %>

            <%# Links para quem ESTÁ LOGADO %>
            <% if locatario_signed_in? %>
              
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownAdmin" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Painel Admin
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdownAdmin">
                  <li><%= link_to "Gerenciar Carros", admin_carros_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Gerenciar Locatários", admin_locatarios_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Gerenciar Empréstimos", admin_emprestimos_path, class: "dropdown-item" %></li>
                </ul>
              </li>

            <% end %>
          </ul>

          <%# Links de Login/Logout na direita %>
          <ul class="navbar-nav">
            <% if locatario_signed_in? %>
              <li class="nav-item">
                <span class="navbar-text text-white me-2">
                  Olá, <%= current_locatario.nome %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to "Minha Conta", edit_locatario_registration_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Sair", destroy_locatario_session_path, class: "nav-link", data: { turbo_method: :delete } %>
              </li>
            <% else %>
              <%# Links para quem NÃO ESTÁ LOGADO %>
              <li class="nav-item">
                <%= link_to "Login", new_locatario_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Cadastre-se", new_locatario_registration_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <%# --- FIM DA BARRA DE MENU --- %>


    <%# --- 2. ÁREA PARA MOSTRAR MENSAGENS (NECESSÁRIO PARA DEVISE) --- %>
    <div class="container mt-3">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
    </div>


    <%# --- 3. CONTEÚDO DA PÁGINA --- %>
    <main class="container">
      <%= yield %>
    </main>
    
  </body>
</html>